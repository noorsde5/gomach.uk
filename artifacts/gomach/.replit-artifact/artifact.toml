kind = "web"
previewPath = "/"
title = "GoMach"
version = "1.0.0"
id = "artifacts/gomach"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21831

[services.development]
run = "pnpm --filter @workspace/gomach run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gomach", "run", "build" ]
publicDir = "artifacts/gomach/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21831"
BASE_PATH = "/"
