name: Deploy GoMach to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # Re-check monthly access at the start of each month (00:05 UTC)
    - cron: "5 0 1 * *"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Check monthly site access
        id: access
        shell: bash
        run: |
          set -euo pipefail
          node scripts/site-access.mjs status >> "$GITHUB_OUTPUT"
          if node scripts/site-access.mjs check; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi
          echo "Site access for $(node scripts/site-access.mjs month): $(grep '^enabled=' "$GITHUB_OUTPUT" | tail -1)"

      - name: Build blocked site (unpaid month)
        if: steps.access.outputs.enabled != 'true'
        run: node scripts/build-blocked-site.mjs

      - name: Setup pnpm
        if: steps.access.outputs.enabled == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Get pnpm store directory
        if: steps.access.outputs.enabled == 'true'
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm dependencies
        if: steps.access.outputs.enabled == 'true'
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        if: steps.access.outputs.enabled == 'true'
        # --no-frozen-lockfile: CI machines may differ from dev (platform-specific binaries)
        # --config.minimumReleaseAge=0: bypass the 1440-min age check that's Replit-dev-only
        run: pnpm install --no-frozen-lockfile --config.minimumReleaseAge=0

      - name: Build GoMach site
        if: steps.access.outputs.enabled == 'true'
        working-directory: artifacts/gomach
        env:
          PORT: "3000"
          # Root base for custom domain (e.g. https://gomach.uk/). Use a subpath only if the site
          # is served from a project URL like https://<user>.github.io/<repo>/.
          BASE_PATH: "/"
          NODE_ENV: production
          VITE_SITE_ACCESS: "yes"
          # Get your free key at https://web3forms.com — enter info@gomach.uk
          # Add it in GitHub: Settings > Secrets > Actions > VITE_WEB3FORMS_KEY
          VITE_WEB3FORMS_KEY: ${{ secrets.VITE_WEB3FORMS_KEY }}
        run: pnpm run build

      - name: Verify Pages artifact
        run: |
          test -f artifacts/gomach/dist/public/index.html
          test -f artifacts/gomach/dist/public/CNAME
          ls -la artifacts/gomach/dist/public | head -20

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: artifacts/gomach/dist/public

  deploy:
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
